- API stateless (file le token du compte habilité à chaque requête)
- mini-lib pour côté serveur cible (le serveur utilisateur du captcha)
- l'envoi des id se fera via le serveur cible : le client send l'id
du captcha au serveur, et le serveur va reroot le captcha token et l'id
sélectionné au serveur captchat, qui va burn la correspondance de sa RAM
et répondre si le guess était valide ou incorrect
